# Local Node server configuration. Copy to .env or export in the shell;
# every value here is optional and harmless. Never commit real secrets.

# HTTP port for the API server.
PORT=8787

# SQLite database location (parent directories are created).
ASCIIWEAVE_DB=data/asciiweave.db

# Build identity reported by /api/health.
GIT_COMMIT=dev
